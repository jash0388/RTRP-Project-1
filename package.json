{
  "name": "sphn",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "concurrently -k -n server,client -c blue,green \"npm --prefix server start\" \"npm --prefix client run preview\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
